---
name: meng-skills
description: "Use when producing, polishing, rewriting, or post-editing formal Chinese professional writing: academic papers, empirical research, grant/project applications, policy/report materials, meeting or interview notes, teaching reform materials, department-management replies, and finance/economics professional analysis. Also use for Chinese academic de-AIGC (降低AIGC检测率/去AI味/知网万方检测). Do not use for casual daily writing, English writing, code, raw data, table headers, citations, policy numbers, or quoted speech."
---

# meng-skills v2

把中文专业文本改成自然、准确、有判断力的书面表达。核心目标：

1. 清除 AI 痕迹；
2. 系统性地重构中文 AI 文本的统计学特征，使其通过 AIGC 检测；
3. 在正式文体边界内保留作者的专业声音。

v2 整合了 chinese-de-aigc 的完整方法论，不再需要单独调用去 AIGC 技能。

本技能不负责日常聊天、朋友圈、私信、生活随笔和英文写作。

## 0. 边界

**触发：**
- 论文、摘要、引言、文献综述、理论分析、实证结果、结论与政策建议；
- 课题申请、项目申报、研究基础、预期成果；
- 会议纪要、座谈纪要、调研报告、工作汇报、政策建议；
- 中文学术评论、专业读书笔记、概念阐释、金融/经济/管理问题分析；
- **降 AIGC 检测率、去 AI 味、知网/万方/维普/Turnitin 中文版检测应对**。

**不触发：**
- 英文输出：走 `humanizer`；
- 日常聊天、私人表达、非专业随笔；
- 搜索词、命令、代码注释、Stata 代码；
- 数据表头、原始统计数据、公式、变量名；
- 引号内原话内容、政策文件名称编号、引用格式、人员姓名职务单位；引号标点本身可统一为全角中文格式。

## 1. 工作流

### 1.1 标准模式（默认，适用于所有场景）

1. **冻结事实**：先标记不能改的内容：数据、引用、政策编号、原话内容、人名、单位、变量名；只允许统一引号标点。
2. **判断文体**：按第 2 节自动路由，不问用户，除非文本同时强命中多个正式模板且目标无法判断。
3. **清 AI 痕迹**：按第 3 节删除套话、拔高、模板腔。
4. **按文体润色**：使用第 4 节规则；命中作者专业表达时读取 `references/professional-voice.md`。
5. **反过度润色**：检查是否把具体事实、判断力、现场感磨平。
6. **朗读检验**：读不顺就改，听起来像新闻稿或申请书模板就重写。

### 1.2 深度模式（当用户明确要求"降 AIGC""去 AI 味""过知网检测"时启用）

五步闭环（整合自 chinese-de-aigc）：

```
┌──────────┐   ┌──────────┐   ┌──────────┐
│1.定位扫描│ → │2.诊断分类│ → │3.差异化改写│
└──────────┘   └──────────┘   └─────┬────┘
                                    │
┌──────────┐   ┌──────────┐         │
│5.二次复查│ ← │4.五维自评│ ←───────┘
└──────────┘   └──────────┘
```

**Step 1 · 定位扫描**：按第 3.1 节五大结构性特征 + 第 3.2 节 AI 痕迹清单做全文扫描，输出结构化问题清单：

```markdown
## AI 痕迹定位报告
| 段落 | 原文片段 | 命中规则 | 严重度 |
|------|---------|---------|--------|
| ¶2   | "毋庸置疑，数字化转型..." | P01 四字套话 | 高 |
| ¶3   | "...此外，该研究还..."    | P04 显性连接词 | 中 |
| ¶5   | "本文认为该机制充分证明..." | P12 绝对化断言 | 高 |
```

⚠️ 不要此时就开始改写，先让用户看到问题全貌。

**Step 2 · 诊断分类**：按段落功能分类，决定改写力度。

| 段落类型 | 示例 | 改写策略 |
|---------|------|---------|
| 事实陈述段 | 方法/数据/结果 | 低调改写，保留学术精确度 |
| 论证段 | 文献综述/讨论/机制分析 | 重点改写，注入"认识论谨慎" |
| 过渡段 | 章节衔接、段落承上启下 | 大幅改写，消除机械连接词 |

**Step 3 · 差异化改写**：针对定位清单逐条修复，遵循四条核心原则：

1. **还原研究者视角**："本研究发现"→"我们用 2015-2023 年 CSMAR 面板数据发现"——加入可验证的颗粒度
2. **制造句长方差**：一段 200 字内强制包含 ≥1 个 ≤15 字短句 + ≥1 个 ≥50 字长句
3. **消灭显性连接词**：去掉"此外/而且/因此/从而"类段首连接词，改用语义接力（下句主语呼应上句宾语关键概念）
4. **绝对→谨慎**："证明/确立/必然/充分说明"→"为……提供了证据/与……的预期一致/可能意味着"

⚠️ 禁止事项：
- 禁止刻意制造错别字或不规范表达
- 禁止使用生僻词、文言化词汇强行降困惑度
- 禁止篡改数据、结论或研究主张——只改表达，不改实质

**Step 4 · 五维自评**：对改写后文本打分（每维 1-10 分）。

| 维度 | 检查点 | 权重 |
|------|--------|------|
| **具体性** | 用具体数据/案例/作者替代模糊表达 | 1.5× |
| **节奏性** | 句长方差 ≥ 150（长短句混排） | 1.2× |
| **谨慎性** | 绝对化断言已降级为条件化表述 | 1.3× |
| **隐衔接** | 段落间消除显性关联词 | 1.0× |
| **研究者语气** | 出现"我们/本团队"等第一人称研究立场 | 1.0× |

加权总分 < 35 → 返回 Step 3 再改。≥ 42 → 通过。

**Step 5 · 二次复查**：冷读者视角三项终审：
1. 通顺性：逻辑链是否连贯？有没有为了降 AIGC 伤到学术表达？
2. 事实性：数据、引用、作者名、年份有没有被误改？
3. 风格一致性：全文语气统一，无"改过 vs 没改过"的段落断层

输出终稿 + 改动摘要（哪些段落改了多少、为什么改）。

## 2. 自动路由

按顺序判断，命中即停止：

| 优先级 | 命中信号 | 路由 |
|---|---|---|
| 1 | 摘要、引言、文献综述、理论分析、研究假设、回归、样本、变量、机制、异质性、稳健性、内生性、DID、PSM、工具变量、固定效应、p 值、R² | 实证论文 |
| 2 | 课题、项目、申报书、社科基金、自然基金、研究基础、研究内容、技术路线、创新之处、预期成果、成果形式 | 课题/项目申请 |
| 3 | 会议纪要、座谈纪要、调研报告、工作汇报、政策建议、学院材料、部门材料、领导讲话、会议指出/强调/要求 | 公文/纪要/报告 |
| 4 | 读书笔记、学术评论、概念阐释、理论阐释、专业分析、金融问题、经济问题、管理问题 | 专业评论/阐释 |

路由 4 必须读取 `references/professional-voice.md`。路由 3 如判断不清，读取 `references/official-doc-example.md`。

## 3. AI 痕迹清单

### 3.1 中文 AI 五大结构性特征（来自 chinese-de-aigc）

不是字词层面的表面修改，而是针对以下五大结构性特征做定点破坏：

| 特征 | 表现 | 检测难度 |
|------|------|---------|
| **四字套话密度过高** | 每 200 字出现 3+ 个"综上所述/不可否认/毋庸置疑/显而易见" | 高 |
| **虚词与关联词冗余** | 过度使用"然而/因此/此外/而且/并且/以及/等等" | 中 |
| **主语回避与隐藏被动** | 大量"本文认为/本研究发现/相关研究表明"式模糊主语 | 中 |
| **句长方差低** | 句子长度集中在 20-35 字，缺乏人类写作的节奏跳跃 | 低（但关键） |
| **结论绝对化** | 倾向使用"证明了/确立了/充分说明/必然导致"等过强断言 | 高 |

### 3.2 具体 AI 痕迹模式

发现以下模式，优先删除或改写：

- **四字套话**：标志着、见证了、体现了、彰显了、综上所述、不可否认、毋庸置疑、显而易见、具有重要意义、奠定基础、深刻影响、关键转折点。
- **宣传腔**：深入交流、广泛探讨、取得显著成效、持续赋能、全面提升、共同推动。
- **模糊归因**：专家认为、行业报告显示、观察者指出、多个来源显示。没有具体来源就改成事实判断或删除。
- **句尾升华**：一句事实后接"这反映了/体现了/说明了……重要性"。
- **先否定再给答案**：不是 X，而是 Y；并非 X，而是 Y；不在于 X，而在于 Y；需要的是 X，而非 Y。多数情况下直接说 Y，或拆为两句独立陈述。企业方原话引号内的"不是"保留不动。
- **机械三段式**：首先、其次、最后硬凑完整；三点并列只为显得全面。
- **同义词乱换**：同一对象反复换称谓。统一术语。
- **填充短语**：值得注意的是、为了实现这一目标、在这个时间点、由于……的事实、在……的情况下。
- **过度限定**：可能潜在地、一定程度上可能、被认为可能会。
- **客服痕迹**：当然可以、这是个好问题、希望对您有帮助、您说得完全正确。
- **引号格式**：中文正文统一使用全角中文引号，一级用""，二级用''；不得使用半角英文引号。
- **格式痕迹**：装饰性粗体、表情符号、引号混用；破折号每千字不超过 2 个。
- **绝对化断言**（深度模式重点检查）：证明了、确立了、充分说明、必然导致、毋庸置疑——降级为：为……提供了证据、与……的预期一致、可能意味着、在一定程度上支持了。
- **显性连接词**（深度模式重点检查）：此外、而且、因此、从而、进而、与此同时——改用语义接力或直接删除。

## 4. 文体规则

### 4.1 通用规则

- 结论先行，原因随后。
- 有判断就明说，不用"需要辩证看待"挡住观点。
- 句子长短交错；连续三句长度接近就拆。
- 说清楚就停，不强行"综上所述""展望未来"。
- 不为顺滑牺牲具体事实；事实比漂亮句子重要。
- 不把所有正式文本都改成公文。

### 4.2 实证论文

- 叙述主体用"本文""本研究"，不用"我""我们"。（深度模式例外：可适度使用"我们"体现研究者立场，降低 AI 检测率）
- 结构保持：引言、理论分析、研究设计、实证结果、机制、异质性、结论与建议。
- 摘要按"问题—样本—方法—发现—机制—贡献"推进，但不要模板化堆满七要素。
- 结果报告要准确：表号、列号、显著性、方向、经济含义。
- 贡献表述可以用"边际贡献"，但不能空泛说"具有重要理论意义"。
- 公司金融问题默认检查：所有制、地区、行业、规模等异质性是否有必要；不要为了完整而硬加。

### 4.3 课题/项目申请

- 开头用现实问题或制度变化切入，再落到研究缺口。
- 文献综述按研究流派组织，不写作者名单堆砌。
- 研究内容写成可执行任务：对象、数据、方法、识别、预期发现。
- 创新点只写真实增量：新问题、新数据、新识别、新机制、新应用。
- 预期成果和社会效益要具体，避免"推动相关领域发展"这类空话。
- 不把申请书写成论文摘要；申请书要让评审看见可行性和完成路径。


### 4.4 公文/纪要/报告

- 先诊断底色：口语叙事强则收紧；AI 套话重则去壳重写。
- 纪要保留事实顺序、主体、动作和企业原话。
- 工作报告用"动作 + 对象 + 目的"，但不要把个人风格改成政府工作报告。
- 部署段可用"要……"，成果段优先用具体数字或事实。
- 无数据支撑时，不写"显著成效""明显提升"。

### 4.5 专业评论/阐释

读取 `references/professional-voice.md`。默认结构：

概念/现象 → 反问或质疑 → 拆解底层逻辑 → 专业判断 → 回到问题本身。

可保留"我以为""我理解""说白了""无非"等表达，但只用于专业阐释，不扩展到日常写作。

## 5. 最终检查

- 是否命中正式专业场景？不命中则不用本技能。
- 是否误把非正式文本改成公文？
- 是否还有 AI 套话、拔高、客服痕迹？
- 是否保留了事实、数据、引用、政策编号、原话？
- 中文正文引号是否统一为全角中文引号？
- 是否有明确判断，而不是只有平滑表述？
- 是否还有现场感：数据、制度、行业、研究对象？
- 是否过度润色，导致原文锋利度和作者位置消失？
- 读起来是否像熟练作者写的，而不是像模板生成的？
- （深度模式额外检查）五维自评加权总分是否 ≥ 42？

## 6. 生成前校验

在正式输出前，额外检查以下两项——来源于实际会话中的返工：

- **专有名词不可盲信用户输入。** 用户原始指令中可能出现笔误。职务名称、机构名称、人员姓名在照搬前做一次基本合理性校验。
- **必须重新加载本技能（skill_view），不可凭记忆。** 记忆中残留的规则不完整。漏加载会导致生成后需修补"并非…而是…""而非…"等 AI 句式。skill_view 是每次文字撰写任务的强制第一步。

## 7. 自动化验证（强制，不可跳过）

生成 docx 或写入最终文本后，立即运行 Python 扫描。违规项必须清零才能交付。技术压力下的人工目视检查不可靠——本条用程序强制执行。

```python
import zipfile, re

def verify_text(full_text):
    violations = []

    # 半角引号
    halfwidth = re.findall(r'["\']', full_text)
    if halfwidth:
        violations.append(f'半角引号 {len(halfwidth)} 处（应为全角 "" 或 '')')

    # 否定句式（排除企业原话引号内）
    neg = re.findall(r'(?:不是|并非|不在于).{0,30}?(?:而是|而在于)', full_text)
    if neg:
        violations.append(f'否定句式 {len(neg)} 处')

    # AI 高频词（含四字套话 + 绝对化断言）
    ai_words = ['此外', '综上所述', '值得注意的是', '标志着', '具有重要意义',
                '希望这对您有帮助', '深入交流', '广泛探讨',
                '毋庸置疑', '不可否认', '显而易见', '充分说明', '必然导致', '证明了']
    for w in ai_words:
        if w in full_text:
            violations.append(f'AI词汇 "{w}"')

    # 破折号每千字 <= 2
    cn = len(re.findall(r'[\u4e00-\u9fff]', full_text))
    if full_text.count('——') > cn / 500:
        violations.append(f'破折号超标')

    # v2 新增：句长方差粗略检查（连续 5 句长度都在 20-35 字区间 = 可疑）
    sentences = re.split(r'[。！？]', full_text)
    mid_range = [s for s in sentences if 20 <= len(s) <= 35]
    if len(mid_range) > len(sentences) * 0.7 and len(sentences) > 10:
        violations.append('句长方差过低（>70%句子集中在20-35字）')

    if violations:
        print('=== VIOLATIONS ===')
        for v in violations:
            print(f'  ✗ {v}')
    else:
        print('✓ meng-skills v2 自动化验证通过')
    return len(violations) == 0
```

docx 文件先用 `zipfile` 解压 `word/document.xml`，提取 `<w:t>` 文本拼接为 `full_text` 再调用 `verify_text()`。

## 8. 朗读测试

听三件事：

1. **呼吸**：不该断的地方断了，补逗号或拆句。
2. **舌头**：念到某词打结，换更自然的词。
3. **耳朵**：像新闻稿、申请书模板或 AI 助手，重写。

---

*本技能 v2 整合了 AERS chinese-de-aigc（48）的五大结构性特征诊断、五步闭环工作流和五维评分体系。原始 17 类模式库详见 `~/hermes/aers/skills/48-copaper-ai-chinese-de-aigc/references/patterns.md`。

## 维护注意事项

⚠️ **双位置同步**：meng-skills 有两个副本，更新时必须同步：

| 位置 | 用途 | 路径 |
|------|------|------|
| Hermes 技能系统 | Agent 运行时加载 | `~/.hermes/skills/writing/meng-skills/SKILL.md` |
| 本地项目仓库 | 版本控制 + GitHub 发布 | `~/hermes/技能/meng skills v2/SKILL.md` |

修改流程：先改 Hermes 版本（`skill_manage`），再 `cp` 到本地仓库。不可只改一个——否则两个副本内容不一致，用户可能用到旧版本。
